# Consolidation Deltas — Architect Reconciliation of the Six Phase Plans

These deltas override the individual phase plans where they conflict. Everything else in each plan stands as written.

1. **Schemas move ownership.** Phase 1 owns the full `server/schemas.ts` → `shared/{domain,contracts,book-status}.ts` move plus the 24-site client predicate adoption (its S5/S6). Phase 2's S1 reduces to: `shared/domain/provider.ts` consolidation (PROVIDERS/MODEL_REGEX dedup from model-client/key-store/schemas). Phase 2's S2 (response contract types, SSE event unions, LibraryBook/BookSummary etc.) stands — it requires route-return analysis P2 already did. Phase 2's S12 "delete schemas.ts shim" is moot: P1 rewrites the 11 server import lines directly, no shim.
2. **Phase 3's shared/ dependencies.** P3's needs #1 (status predicates) and the raw entity schemas (#3, #4, #7, part of #9) come from Phase 1. The response/SSE contract types (#2, #5-serverside, #6, #8, #10) come from Phase 2 S2. Execution order: Phase 2 lands S1+S2+S3 as an early "phase-2-foundations" PR merged to master before slice work; Phase 3 rebases on it. Phase 3 S1 (http/sse/urls/constants) may start immediately after Phase 1 merges. P3 moves `GenerationEvent` out of `parse-sse-stream.ts` itself unless P2 S2 already placed it.
3. **Predicate sweep dedup.** Phase 1 S6 performs the 24 status-literal replacements. Phase 3 S12 becomes: timing-constants sweep + the rg gates as verification (expect zero remaining literals, fix stragglers only).
4. **buildServer chain.** Phase 0 S1 extracts `buildServer()` (no listen, no recoverFromCrash — that exclusion is load-bearing: P4's routes-doc generator and all inject tests depend on it). Phase 2 S6a evolves it to accept `overrides?: Partial<Ports>` via composition root. Phase 4 S4 consumes it and must NOT re-extract.
5. **CI ownership chain.** Phase 0 S6 creates `.github/workflows/ci.yml` (install, typecheck, lint, test). Phase 4 S5 appends the routes-doc drift job. Phase 5 S5 appends knip and confirms branch protection. Nobody recreates the file.
6. **Planning docs.** Phase 0 S8's deletion list stands, but each phase PR commits its own reconciled plan into `docs/plans/refactor/phase-N.md`, and Phase 0 also commits `master-plan.md` + this consolidation doc. P0's acceptance becomes `ls docs` → `plans screenshots`. Phase 4 S1 must NOT delete `docs/plans/refactor/`.
7. **Additional early hygiene (moved from Phase 5 S4 into Phase 0):** delete tracked `.superpowers/`, `.agents/`, `skills-lock.json`; delete stray `pnpm-workspace.yaml` (invalid string-form duplicate of package.json's `onlyBuiltDependencies` — verify `pnpm install` still builds the native deps afterward, since workspace-file presence affects pnpm's build-script approval). Phase 5 keeps the verification greps.
8. **lefthook discrepancy.** P0 baseline says no lefthook.yml; P1 recon says one exists running `npx tsc`. Phase 0 must glob for any lefthook config (`lefthook.yml`, `.lefthook.yml`, `.config/lefthook.yml`, package.json key) before writing one.
9. **TDD mandate (owner requirement, binding all phases).** Tests land before or with the code they cover, visible in commit order: characterization tests before refactors (P0 ✓ by construction), contract tests + fakes before adapters (P2 S4 before S5 ✓), service unit tests written red against the target service signature before extraction turns them green (P2 S7-S11), API-client module tests before/with each module and dialog-reducer tests before the reducer (P3 S1/S2/S10), predicate tests before `shared/book-status.ts` lands (P1 S5). Commit messages: `test:` commits precede or accompany `refactor:`/`feat:` commits. No jsdom/@testing-library this effort — node-env unit tests only (P3 risk 8 decision affirmed).
10. **Merge protocol.** Rebase-and-merge (linear history), never squash — squashing would destroy the visible test-first commit ordering that is part of the demo. Conventional commits. One PR per phase (plus the small phase-2-foundations PR), each PR body lists gate evidence and any frozen quirks.
11. **Sanctioned behavior changes (the only two):** (a) eight MCP authoring routes 500→400 on invalid input (P2 risk 4) — accepted, document in PR, single sanctioned P0-assertion edit; (b) `useBackgroundTasks` SSE reconnect churn reduction (P3 risk 4) — accepted as invisible improvement, called out in PR.
12. **Port catalog is Phase 2's** (11 ports + optional Clock/AudioAssembly, both approved — Clock kills 15 raw timestamps and enables exact-time service tests). Phase 4 documents whatever P2 ships (its "EpubIO" name yields to P2's EpubImport/EpubExport).
13. **Worktree safety.** `.claude/worktrees/*` may contain full repo copies; every codemod (ts-morph etc.) must scope to the tsconfig project, never walk the filesystem. All implementers get this warning.
14. **Gated-outward items resolved by owner during grilling:** talks → new public repo `rsml/talks` linked from README (approved); six phase-tracking GitHub issues replace the empty todos.yaml conversion (approved substitute); phase PRs pushed to origin as part of normal flow. Log each in the phase report.
15. **Node/tooling pins:** CI node 24 (matches AbortSignal.any usage), pnpm from packageManager field. `ELECTRON_SKIP_BINARY_DOWNLOAD=1` in CI. macOS runner fallback per P0 S6 ladder if native deps fail on Linux.
