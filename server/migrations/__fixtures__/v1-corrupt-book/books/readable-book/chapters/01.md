# A Book That Reads Fine

One chapter of ordinary content, present so this fixture book is a complete
book directory rather than a bare `meta.yml`. Its neighbour in this fixture
is deliberately broken.
